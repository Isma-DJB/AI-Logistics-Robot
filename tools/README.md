# Diagnostic Tools

The hardware diagnostic commands are intentionally safe placeholders in I-0.1. They perform no camera, motor, or calibration operation and return exit code `2`.

Their implementations are scheduled for Implementation Draft I-0.9, after the physical components and safety procedures have been verified.